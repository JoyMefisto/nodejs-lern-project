extends ../_layout

block content
    form(method='post')
        p Вы уверены что хотите удалить Команду "#{tournament.name || 'text'}"
        .extra.content
            .ui.two.buttons
                a.ui.basic.green.button(href=`/admin/${game}/tournaments`) Нет
                button.ui.basic.red.button(type='submit') Да